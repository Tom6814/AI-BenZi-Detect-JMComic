- [ ] Vue3 前端和 FastAPI 后端项目基础架构搭建成功，可互相通信。
- [ ] 前端应用了 Material 3 Expressive 和 Aurora Neon/Liquid Glass 的整体视觉风格。
- [ ] AI API 配置模块能正确连接并验证密钥（兼容 OpenAI 和 Gemini）。
- [ ] 针对深度思考模型（如带有 `<think>` 标签），后端能够成功剥离思考过程并返回干净数据。
- [ ] 搜索框支持根据纯数字和文本输入自动切换检索策略（列表展示或快速锁定）。
- [ ] 【避雷清单】和【喜欢清单】能够成功保存，且在 UI 上有强烈的红绿视觉区分。
- [ ] 后端完整应用了提供的 System Prompt，并能成功整合本子的封面（Base64/URL）等多元信息发给大模型。
- [ ] 后端正确开启了 AI 调用的 JSON 模式并返回符合结构的 JSON 对象。
- [ ] 前端鉴定报告成功渲染，避雷命中为高警示度红色/霓虹红，喜欢命中为绿色/霓虹绿，未命中为半透明玻璃态。
- [ ] `reasoning` 字段通过 Markdown 解析器正确渲染，且首句使用了加大加粗的引语样式（Blockquote）。
